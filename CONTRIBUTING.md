# Contributing

欢迎提交新的圆屏应用、驱动修复、服务端集成和测试。

1. 从最新 `main` 创建短分支。
2. 保持模块边界，不在新应用中硬编码主机、密钥或 Wi-Fi。
3. 为输入边界、失败路径和持久化行为补测试。
4. 运行 README 中的测试与 `idf.py build`。
5. 暂存变更后运行 `python tools/check_public_tree.py`。
6. PR 描述写清真机型号、验证步骤、内存/镜像变化和已知限制。

不要提交生成目录、真实证书、`clock_secrets.h`、服务器环境文件、用户照片、任务数据或真机取证产物。对 UI 的改动请附脱敏截图；对协议的改动同时更新设备、服务端和文档。

代码风格以邻近文件为准。优先小而可验证的修改；不要顺手重构无关模块。安全问题请按 `SECURITY.md` 私下报告。
